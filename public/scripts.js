document
    .querySelector('header button')
    .addEventListener("click", function() {
        document.querySelector('.form')
        /* Adiciona uma nova classe chamada hide */
        .classList.toggle('hide')
    })
